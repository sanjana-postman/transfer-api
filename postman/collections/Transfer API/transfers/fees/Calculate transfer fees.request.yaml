$kind: http-request
url: "{{baseUrl}}/transfers/fees"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Calculate transfer fees.resources/examples
order: 1000
